import React from "react";
import { Paper, Typography } from "@mui/material";

const DepartamentosPage: React.FC = () => (
  <Paper sx={{ p: 2 }}>
    <Typography variant="h6" fontWeight={700}>Departamentos (stub) ?</Typography>
  </Paper>
);

export default DepartamentosPage;
