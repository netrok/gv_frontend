import React from "react";
import { Paper, Typography } from "@mui/material";

const PuestosPage: React.FC = () => (
  <Paper sx={{ p: 2 }}>
    <Typography variant="h6" fontWeight={700}>Puestos (stub) ?</Typography>
  </Paper>
);

export default PuestosPage;
